## 目标
- 把目前在 `pplo-elsarticle.tex` 中的公式 `X(i,j) &= ... , i &= ...` 改为两行显示，避免在两栏 PDF 中一行排不下导致溢出。
- 保持 elsarticle（5p 两栏）版式规范、对齐美观、可选保留或取消公式编号。

## 修改方案
1. 在当前位置（你指出的第 154 行附近）把单行改为 AMS 对齐环境，两行拆分：
```
\begin{align}
X(i,j) &= \mathrm{LB}(j) + R\,\big(\mathrm{UB}(j) - \mathrm{LB}(j)\big),\\
& i=1,\ldots,N,\quad j=1,\ldots,D
\end{align}
```
- 要点：用 `\\` 强制换行；第二行用对齐符号 `&` 与第一行的等号对齐；在两栏文稿中更易避免 Overfull hbox。

2. 如果不需要编号，用无编号的版本：
```
\[
\begin{aligned}
X(i,j) &= \mathrm{LB}(j) + R\,\big(\mathrm{UB}(j) - \mathrm{LB}(j)\big),\\
& i=1,\ldots,N,\quad j=1,\ldots,D
\end{aligned}
\]
```
- 要点：`\[ ... \]` 取消编号；`aligned` 保留等号对齐。

3. 版式一致性微调：
- 在第二行把域说明前的逗号移到上一行末尾（已示例），避免行首逗号；
- 在 `R` 前加细空格 `\,`，提升排版观感；
- 保持使用已加载的 `amsmath`，不引入额外包。

## 验证计划
- 重新编译两次以稳定引用；
- 检查该公式所在页是否不再出现溢出警告（Overfull hbox），并确认两行断行效果在 PDF 中清晰可读；
- 若仍有拥挤，进一步把 `R` 周围的括号改为乘号 `\times` 或在长项前后断词加 `\,` 以减小行宽。

请确认是否采用“有编号（align）”还是“无编号（aligned+\[\]）”版本，我将据此应用修改并编译验证。