## 定位
- 目标位置：`\label{eq:next-index}` 所在的公式块之后（见 pplo-elsarticle.tex:287–290）。该式为并行迁移索引 `next_g` 的定义，对应文中“公式（20）”。

## 插入方式
- 使用非浮动的居中块 + `\captionof{figure}`（已加载 `capt-of` 包），确保图片紧随公式出现且不与版式冲突；保持 5p 双栏宽度规范。

## 具体代码（紧跟在 `\end{equation}` 之后插入）
```latex
\begin{center}
  \includegraphics[width=\columnwidth]{pplo_figures/Parallel Migration.png}
  \captionof{figure}{Parallel migration among sub-swarms: each group's $gBest_g$ replaces the next group's worst}
  \label{fig:parallel-migration}
\end{center}
```

## 说明
- 不新增任何宏包；图片路径使用现有 `pplo_figures/Parallel Migration.png`。
- `\columnwidth` 保持双栏版式；如需更紧凑可改为 `0.95\columnwidth`。
- 若改用浮动体，可替换为：
```latex
\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{pplo_figures/Parallel Migration.png}
  \caption{Parallel migration among sub-swarms}
  \label{fig:parallel-migration}
\end{figure}
```
但浮动体位置可能上移或下移，非浮动能保证“紧跟公式（20）”。

请确认后我将按上述方案插入并验证编译。