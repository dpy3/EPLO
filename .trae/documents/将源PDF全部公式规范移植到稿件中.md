## 工作范围
- 从 `PPLO A Parallel Polarization Learning Optimizer .pdf` 提取全部公式，移植到 `pplo-elsarticle.tex`。
- 版式严格遵循 `elstest-5p.pdf` 的 5p 两栏规范（字号、对齐、换行、编号、引用）。
- 保持与现有符号表 `tab:notation` 一致的记号与含义。

## 公式整理与映射
- 建立“章节 → 公式列表”的映射，按 PDF 中的章节顺序组织：
  - 1.2 Problem formulation：目标函数、归一化、边界罚项、综合适应度、变量约束与投影/夹紧。
  - 1.3 Update rules：位置/速度更新、参数随迭代的调度（`w(t)`,`c1(t)`,`c2(t)`）、对立学习候选与概率、接受准则。
  - 1.4 Learning strategies：Lévy 扰动的形式、有限差分梯度估计与范数、局部步更新、高斯噪声注入、`pBest/gBest/globalBest` 更新准则。
  - 其它：如 PLO 的螺旋/椭圆运动模型、碰撞/干扰项的具体表达（若在 PDF 中给出明确公式），统一移植到“PLO 数学模型与伪代码”小节。
- 为每条公式拟定语义清晰的旁文与符号解释，避免仅给出公式而无语境。

## LaTeX 表达与版式规则
- 环境选择：
  - 优先使用 `align`/`aligned`（已启用 `amsmath`），保证等号对齐与两栏断行友好。
  - 长公式分行：用 `\\` + 左端对齐（次行用 `&` 对齐到等号），必要时加 `\allowbreak` 在长项前缀处。
  - 条件域说明（如 `i=1,...,N`、`j=1,...,D`）单独一行或与主公式用 `\quad` 分隔，避免溢出。
- 编号与引用：
  - 每个关键公式加 `\label{eq:...}`；文内采用 `Eq.~\ref{eq:...}` 引用。
  - 同组公式采用 `align` 连续编号；非关键推导用无编号 `\[ ... \]` 或 `aligned` 置于编号环境内部。
- 记号一致性：
  - 保持现有 `\mathrm{LB}(j),\mathrm{UB}(j),\mathrm{rand}` 等风格；向量/矩阵用粗体 `\mathbf{x}`；逐元素乘法用 `\circ`；范数用 `\|\cdot\|`。
  - 必要处添加简短解释句，引用 `tab:notation`，不重复定义。
- 两栏规范：
  - 长公式拆行避免 Overfull hbox；在两栏顶部适度插入公式，避免与图表/表格拥挤。
  - 维持现有字号（正文 10pt），公式默认字号由类控制，不额外缩小，保证可读性。

## 文档结构调整（不改变章节语义）
- 在已存在的：
  - `\subsection{Problem formulation}` 下补齐 1.2 全部公式与解释。
  - `\subsection{Update rules of PPLO}` 下补齐 1.3 全部更新与调度公式。
  - `\subsection{Learning strategies and enhancements}` 下补齐 1.4 全部增强策略公式。
- 为每个新公式块添加一两句解释文字（不冗长），保证读者上下文理解。

## 一致性与质量控制
- 方程检查：符号一致、维度一致、边界/概率范围声明清楚。
- 断行与对齐验证：确保所有长式在两栏中无溢出；必要时将条件域移至下一行对齐。
- 交叉引用：在首次出现处引用符号表；在方法小节中引用关键等式编号。

## 交付与验证
- 双次编译验证，清除可能的引用警告；扫描日志中 `Overfull/Underfull`，对个别长式微调断行位置。
- 截图或标注页码位置，确认公式均放置在相关段落后，不与正文/图表冲突。

## 可选增强（征求同意后执行）
- 引入 `mathtools`（若允许）以获得更好的对齐控制与 `\coloneqq` 等符号。
- 为常用项定义轻量宏（如 `\LB_j`、`\UB_j`、`\rand`），提升可维护性。

请确认以上计划，我将按此逐条移植并验证，确保所有 PDF 公式完整、清晰地出现在稿件中并符合 5p 两栏规范。