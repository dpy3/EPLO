## 目标
将图片中的传动比以“行文+行内数学”的形式提供 LaTeX 代码，不使用 equation/align 环境，避免与正文冲突。

## 放置位置建议
- 小节：“Planetary Gear Train Design Optimization Problem → Mathematical Model”。
- 直接作为一句行文插入到传动比介绍处（或替换现有对齐公式）。

## 行文代码（可直接粘贴）
```latex
Transmission ratios: $i_{1}=\frac{N_{6}}{N_{4}}$, $i_{01}=3.11$, $i_{2}=\frac{N_{6}(N_{1}N_{3}+N_{2}N_{4})}{N_{1}N_{3}(N_{6}-N_{4})}$, $i_{0R}=-3.11$, $I_{R}=-\frac{N_{2}N_{6}}{N_{1}N_{3}}$, $i_{02}=1.84$.
```

## 一致性
- 使用行内 `$...$` 数学，变量与下标与文档一致（`N_{i}`、`i_{1}`、`i_{01}` 等）。
- 不含标签与编号，避免引用冲突。

## 说明
- 该写法在双栏版式中不会溢出，并与段落自然衔接。